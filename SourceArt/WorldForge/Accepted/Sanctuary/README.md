# Accepted Sanctuary sources

Only a dependency-closed WorldForge transaction may create a version directory here. Accepted versions are immutable, LFS-covered, and retained during pointer rollback. The checked-in 1.0.0 intent names every allowed source destination; this directory intentionally contains no generated art yet.
